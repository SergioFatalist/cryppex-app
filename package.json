{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "start:dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview && wrangler pages dev",
    "postinstall": "nuxt prepare",
    "deploy": "npm run build && wrangler pages deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "nuxt": "^3.12.2",
    "vue": "latest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "nitro-cloudflare-dev": "^0.1.4",
    "wrangler": "^3.62.0"
  }
}
